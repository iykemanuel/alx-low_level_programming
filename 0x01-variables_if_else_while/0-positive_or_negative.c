#include <stdio.h>
/**
 * main - print if the number is positive, zero or negative
 *
 * Return: Always 0 (success)
 */
int main(void)
{
char n;
for (n = 'a' ; n <= 'z' ; n++)
{
	putchar(n);
}
putchar('\n')
return (0);
}
